house TestHouse {
    global joinCounter: int;
    global quitCounter: int;

    handle JOIN {
        global.joinCounter = global.joinCounter + 1;
    }

    handle QUIT {
        global.quitCounter = global.quitCounter + 1;
    }
}