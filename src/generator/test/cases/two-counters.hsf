house TestHouse {
    global joinCounter: int = 0;
    global quitCounter: int = 0;

    handle JOIN {
        global.joinCounter = global.joinCounter + 1;
    }

    handle QUIT {
        global.quitCounter = global.quitCounter + 1;
    }
}