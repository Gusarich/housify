house TestHouse {
    global counter: int;

    handle JOIN {
        let x: int = 5;
        let y: int = 2;
        let z: bool = true;
        if (x == 5) {
            global.counter = 10;
        } else if (y == 2) {
            global.counter = 20;
        } else if (z) {
            global.counter = 30;
        } else {
            global.counter = 40;
        }
    }
}