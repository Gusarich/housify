house TestHouse {
    global counter: int;

    handle JOIN {
        let x: int = 5;
        let y: int = 2;
        let z: bool = true;
        if (x + y == 7) {
            global.counter = 10;
        } else {
            global.counter = 20;
        }
    }
}