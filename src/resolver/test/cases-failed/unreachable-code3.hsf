fun testFunction(x: int): int {
    let y: int = x + x;
    if (x > 0) {
        if (x > 0) {
            return y;
        } else {
            return y;
        }
    } else {
        return y;
    }
    y += 2;
}