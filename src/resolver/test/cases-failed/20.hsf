house MyHouse {
    handle(event: BlockBreak) {
        let x: int = 5;
        if (x == 5) {
            let y: int = 10;
            if (y == 10) {
                let z: int = 20;
                if (z == 20) {
                    let a: int = 30;
                    if (a == 30) {
                        let b: bool = 40;
                    }
                }
            }
        }
    }
}
