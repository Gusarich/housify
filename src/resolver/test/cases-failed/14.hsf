house MyHouse {
    handle(event: BlockBreak) {
        let x: int = 10;
        if (x == 10) {
            if (x == 20) {
                x = 30;
            } else {
                x = true;
            }
        }
    }
}
