house House1 {
    global x: int;

    handle JOIN {
        let x: int = 1;
        global.x = global.x + x;
    }
}

house House2 {
    global x: int;
    global y: int;

    handle JOIN {
        let x: int = 1;
        global.x = global.x + x;
        global.y = global.y + 1;
    }
}