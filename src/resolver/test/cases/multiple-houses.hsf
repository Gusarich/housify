house House1 {
    global x: int = 1;

    handle JOIN {
        let x: int = 1;
        global.x = global.x + x;
    }
}

house House2 {
    global x: int = 1;
    global y: int = 0;

    handle JOIN {
        let x: int = 1;
        global.x = global.x + x;
        global.y = global.y + 1;
    }
}