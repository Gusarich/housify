house MyHouse {
    handle BlockBreak {
        let x: int = 10;
        if (x == 10) {
            if (x == 20) {
                x = true;
            } else if (x == 30) {
                x = 40;
            } else {
                x = false;
            }
        }
    }
}
